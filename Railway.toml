# Railway.toml configuration for the backend API

[build]
builder = "NIXPACKS"

[deploy]
# Build and start the server (no Prisma/database needed)
startCommand = "npm run build && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30000
