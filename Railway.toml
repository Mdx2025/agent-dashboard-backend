[build]
builder = "NIXPACKS_V2"
buildCommand = "npm install"
root = "."

[build.env]
NODE_ENV = "production"
NIXPACKS_NODE_VERSION = "20"

[deploy]
startCommand = "npm start"
restartPolicy = "always"

[start]
cmd = "npm start"
